/**
 * Public profile URLs. These are emitted as `sameAs` in the Person structured
 * data, which is how Google links this site to you as a real-world entity.
 *
 * Only add URLs that actually resolve and that clearly belong to you — a dead
 * or wrong link here is worse than no link at all. Leave an entry empty to
 * omit it.
 */
export const socialProfiles: string[] = [
    'https://github.com/Hull95',
    '', // LinkedIn — e.g. https://www.linkedin.com/in/your-handle
].filter(Boolean);